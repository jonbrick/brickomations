<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.brickomations.daily-brief</string>

    <!-- Wraps `node cli/daily-brief.js` in caffeinate so the system holds awake -->
    <!-- for exactly the script's lifetime. Wakelock releases on script exit.   -->
    <key>ProgramArguments</key>
    <array>
        <string>__HOME__/projects/brickomations/scripts/run-with-wakelock.sh</string>
        <string>/opt/homebrew/bin/node</string>
        <string>__HOME__/projects/brickomations/cli/daily-brief.js</string>
    </array>

    <!-- Single fire at 5:50 AM — separate from `yarn sync`.                      -->
    <!-- Reads existing data/*.json (refreshed by 11 PM sync the night before),   -->
    <!-- scans active project folders for `morning_brief:` frontmatter, writes    -->
    <!-- data/briefs/<today>.json. ~5 sec runtime. Lands ~10 min before the 6 AM  -->
    <!-- Cowork morning-brief task that consumes the brief.                       -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key><integer>5</integer>
        <key>Minute</key><integer>50</integer>
    </dict>

    <key>StandardOutPath</key>
    <string>__HOME__/projects/brickomations/local/logs/launchd-stdout.log</string>

    <key>StandardErrorPath</key>
    <string>__HOME__/projects/brickomations/local/logs/launchd-stderr.log</string>

    <key>WorkingDirectory</key>
    <string>__HOME__/projects/brickomations</string>
</dict>
</plist>
